pipeline {
    agent {
        docker { image 'jmeter' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'pwd'
                sh 'cd jmeter'
                sh 'ls -lrt'
                sh 'pwd'
                sh 'cd jmeter'
                sh 'ls -lrt'
                sh 'pwd'
            }
        }
    }
}
