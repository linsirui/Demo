pipeline {
    agent {
        docker { image 'jmeter' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'pwd jmeter'
                sh 'cd jmeter'
                sh 'ls -lrt'
                sh 'cd jmeter'
                sh 'ls -lrt'
            }
        }
    }
}
